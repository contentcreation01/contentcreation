# ==============================================================================
# Python Dependencies — AI Content Creation Pipeline & Strategy War Room
# ==============================================================================
# Core pipeline dependency (required):
yt-dlp>=2024.08.0

# Optional: Enhanced download formats & metadata (already bundled with yt-dlp)
# mutagen>=1.47.0     # Audio tag writing (optional, used for MP3 metadata)

# All other functionality uses Python standard library only:
# - http.server       (Studio Dashboard web server)
# - subprocess        (FFmpeg, Whisper CLI, yt-dlp invocation)
# - threading         (Async pipeline runner)
# - json, os, re      (File I/O and data handling)
# - pathlib           (Cross-platform path handling)
# - mimetypes         (Media type serving)
# - argparse          (CLI argument parsing)
# - urllib.parse      (URL decoding/parsing)
# - datetime          (Session timestamping)
# - sys, platform     (Cross-platform OS detection)

# ==============================================================================
# System-level dependencies (installed via setup scripts, NOT pip):
# ==============================================================================
# FFmpeg >= 6.0         macOS: brew install ffmpeg | Windows: winget install Gyan.FFmpeg
# yt-dlp binary         macOS: brew install yt-dlp | Windows: pip install yt-dlp
# whisper-cpp binary    macOS: brew install whisper-cpp | Windows: whisper.cpp/releases
# ==============================================================================
